#!/data/data/com.termux/files/usr/bin/bash

echo "=== LinxMint Doctor ==="
echo

echo -n "[1] proot-distro: "
command -v proot-distro >/dev/null && echo "OK" || echo "FEHLT"

echo -n "[2] Debian: "
echo -n "[2] Debian: "
check_debian && echo "OK" || echo "FEHLT"

echo -n "[3] Konfig: "
[ -f "$HOME/LinxMint/config/linx.conf" ] && echo "OK" || echo "FEHLT"

echo
echo "Status: CHECK COMPLETE"
echo
echo "=== System Summary ==="
echo "LinxMint OK (basic checks completed)"
if [ "$1" = "--fix" ]; then
    echo "Auto-fix mode (coming soon)"
fi
